name: FQT OSV4 IP04A Continuous Parity 20260812
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Payload staging only; no execution on push."
