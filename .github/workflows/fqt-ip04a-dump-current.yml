name: FQT IP04A Dump Current Chunks
on:
  push:
    branches:
      - fqt-ip04a-dump-current-20260812
    paths:
      - '.github/workflows/fqt-ip04a-dump-current.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  dump:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Normalize chunks
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dump
          python - <<'PY'
          import hashlib,json,pathlib
          rows=[]
          for p in sorted(pathlib.Path('fqt_ip04a').glob('payload.b64.*')):
              s=''.join(p.read_text().split())
              q=pathlib.Path('dump')/(p.name+'.normalized.txt')
              q.write_text(s)
              rows.append({'name':p.name,'chars':len(s),'sha256':hashlib.sha256(s.encode()).hexdigest()})
          pathlib.Path('dump/manifest.json').write_text(json.dumps(rows,indent=2)+'\n')
          print(json.dumps(rows,indent=2))
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: fqt-ip04a-current-chunks
          path: dump/**
          retention-days: 1
          if-no-files-found: error
