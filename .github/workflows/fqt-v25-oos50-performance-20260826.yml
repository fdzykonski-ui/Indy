name: FQT V25 OOS50 Performance 20260826 (Archived)

# V25 depended on a short-lived predecessor artifact.  The active, fail-closed
# successor is .github/workflows/fqt-v26-oos50-repair-20260827.yml, which uses
# an immutable provenance receipt plus freshly executed native correctness
# gates.  Keep V25 manually inspectable but never trigger it on source pushes.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-contract:
    runs-on: ubuntu-latest
    steps:
      - name: Explain successor contract
        run: |
          echo 'V25 is archived because its Iteration-3B Actions artifact expired.'
          echo 'Run FQT V26 OOS50 Data Contract Repair 20260827 instead.'
